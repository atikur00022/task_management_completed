import React from 'react';

const ForgetPassword = () => {
    return (
        <div>
            this is forget password component and lazy is added
        </div>
    );
};

export default ForgetPassword;